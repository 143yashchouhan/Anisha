
VanillaTilt.init(document.querySelector(".card_1"), {
    max: 35,
    speed: 400,
    glare: true,
    "max-glare": .4,
});


VanillaTilt.init(document.querySelector(".card_2"), {
    max: 35,
    speed: 400,
    glare: true,
    "max-glare": .4,
});


VanillaTilt.init(document.querySelector(".card_3"), {
    max: 35,
    speed: 400,
    glare: true,
    "max-glare": .4,
});


VanillaTilt.init(document.querySelector(".card_4"), {
    max: 35,
    speed: 400,
    glare: true,
    "max-glare": .4,
});


VanillaTilt.init(document.querySelector(".card_5"), {
    max: 35,
    speed: 400,
    glare: true,
    "max-glare": .7,
});
